
search() #gibt installierte pakete aus.

install.packages("stringr") #paket stringr installieren

install.packages(c("ggvis","ggplot2")) #mehrere pakete installieren c;combine

update.packages(ask=FALSE) #Pakete aktualisieren ohne nachzufragen

library(stringr) #Paket in den Workspace einbinden